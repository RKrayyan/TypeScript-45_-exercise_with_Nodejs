var user = ['rayyan', 'rashid', 'farhan', 'noman', 'armas'];
if (user.length == 5) {
    console.log("We need to find some users!");
}
else {
    user = [];
    console.log("All user have been removed" + user.length);
}
